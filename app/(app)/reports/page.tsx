import { GlobalCapabilityPlaceholder } from "@/components/global-capability-placeholder";

export default function ReportsPage() {
  return (
    <GlobalCapabilityPlaceholder
      title="Reports"
      description="Generate delivery and governance reporting across projects, portfolios, and assurance activity."
      highlights={["Executive packs", "Operational dashboards", "Regulatory extracts"]}
    />
  );
}
