import { ProjectModulePlaceholder } from "@/components/project-module-placeholder";

export default function ProjectPlanPage() {
  return (
    <ProjectModulePlaceholder
      title="Plan"
      description="Capture scope assumptions, timeline baselines, and planning checkpoints."
      nextFocus="Add schedule baselines and dependency tracking."
    />
  );
}
