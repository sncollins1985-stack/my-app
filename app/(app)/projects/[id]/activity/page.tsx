import { ProjectModulePlaceholder } from "@/components/project-module-placeholder";

export default function ProjectActivityPage() {
  return (
    <ProjectModulePlaceholder
      title="Activity"
      description="Review cross-module project events, updates, and timeline history."
      nextFocus="Add audit-ready event filters and actor context."
    />
  );
}
